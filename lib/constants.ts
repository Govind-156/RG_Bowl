/** Flat delivery charge in INR (BTM 2nd Stage). */
export const DELIVERY_CHARGE = 20;

/** First-order referral discount as decimal (0.25 = 25% off subtotal). */
export const REFERRAL_FIRST_ORDER_DISCOUNT = 0.25;

/** Keywords used to validate that an address is inside the delivery area (BTM). */
export const DELIVERY_ALLOWED_AREA_KEYWORDS = ["BTM", "BTM Layout"];
